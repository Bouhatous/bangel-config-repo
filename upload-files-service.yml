spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: -1
      max-request-size: -1

server:
  port: 8098

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
file:
  upload-dir: C:\\server\\uploads
